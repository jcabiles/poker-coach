VERDICT: REJECT

1. **The 200-hand gate is off by one at the exact transition and is not specified as a server-side deal barrier.** Evidence: `docs/ai-dlc/specs/two-mode-simulate.md:56-57`; `backend/app/services/sim_session.py:924-932,1398-1412`; `frontend/src/components/SimulateView.tsx:492-506`. When hand 200 settles, `hand_no` remains 200 and completed hands equal 200, not 199; additionally, Watch-off immediately deals the next hand without adopting the settled response. The correct derivation is status-dependent, and `deal_next_hand()` must refuse to advance while the check is pending. No new count column is needed. **Severity: blocking.**

2. **The blind-check endpoint has no defined idempotency or concurrency contract.** Evidence: `docs/ai-dlc/specs/two-mode-simulate.md:63-66,86-95`; `backend/app/api/v1/simulate.py:86-104`. The plan does not define the request fields, skip representation, response, error statuses, validation of the three expected seat IDs, or what happens on duplicate/conflicting submissions. Two tabs—or a retry after a lost response—could overwrite the stored score, while a concurrent next-hand request could race the submission. First-write/duplicate behavior and transactional deal gating must be specified and tested. **Severity: blocking.**

3. **No fifth static archetype render site exists, but the post-unlock range panel creates an unhandled leak transition.** Evidence: the four cited sites resolve at `frontend/src/components/simulate/SimTable.tsx:291-294`, `SimLedger.tsx:60`, `SimVillainRange.tsx:55-79`, and `SimRangeChart.tsx:212-220`; the panel remains mounted solely from `frontend/src/components/SimulateView.tsx:839-847`. If the user opens a range and then chooses “Labels: Hidden,” hiding only the button leaves the already-open panel and its persona header visible. The plan must close or gate the mounted panel, including in-flight responses. **Severity: blocking.**

4. **The file whitelist makes the definition of done impossible to satisfy.** Evidence: `docs/ai-dlc/specs/two-mode-simulate.md:97-113,168-180,184-187`; `frontend/src/api/client.ts:105-107`. `frontend/src/api/client.ts` is omitted even though session creation must send a mode and the blind-check POST needs a client call. Required backend tests are also outside the list, while the roadmap update explicitly required at line 187 is outside it too. This directly contradicts “nothing outside the files named.” **Severity: blocking.**

5. **The new wire contract is ambiguous and its declared route contradicts the existing route family.** Evidence: `docs/ai-dlc/specs/two-mode-simulate.md:82-95`; `backend/app/api/v1/simulate.py:71-108`; `frontend/src/api/client.ts:105-107`. The spec does not say whether mode is a JSON body or query parameter, does not define a create request schema/default for old callers, and types mode only as unrestricted `str`. It also declares plural `/simulate/sessions/{id}/blind-check`, while all session routes use singular `/simulate/session/{id}`. Builders could produce incompatible clients or persist invalid modes. **Severity: should-fix.**

6. **The mode-choice lifecycle lacks regression coverage for the two existing eager-create paths.** Evidence: required behavior is at `docs/ai-dlc/specs/two-mode-simulate.md:30-34`, while current boot recovery creates immediately at `frontend/src/components/SimulateView.tsx:398-425` and Leave Table ends then immediately creates at `:549-566`; planned verification covers only starting Challenge at `docs/ai-dlc/specs/two-mode-simulate.md:177-180`. Without explicit tests, stale-session recovery or leaving can silently create Training instead of returning to mode choice, while a reload can briefly re-ask during restore. **Severity: should-fix.**

7. **The active-session migration claim is not backed by an upgrade-from-populated-database test.** Evidence: `docs/ai-dlc/specs/two-mode-simulate.md:82-85`; migrations run before serving at `backend/app/main.py:17-20`; the existing migration test only upgrades a clean database at `backend/tests/test_db.py:8-16`. SQLite requires careful ordering/default handling when adding a non-null column to populated tables. Test an active pre-migration session upgraded from revision 0014, restored as Training with its hand unchanged. An HTTP request cannot be in flight during startup migration, but an active persisted session can. **Severity: should-fix.**

8. **The deterministic seat-selection contract does not guarantee three distinct seats or stability across process restarts.** Evidence: `docs/ai-dlc/specs/two-mode-simulate.md:61-62,171-174`; the actual roster is eight permanent non-hero seats at `backend/app/domain/table/play.py:44-61`. Repeated modulo picks can collide, and Python’s salted `hash()` can change after restart; the proposed test checks only repetition and exclusion of hero. Specify a stable digest and selection without replacement, and assert `len(set(seats)) == 3`. Busted/left seats are not an actual concern: all nine seats persist and are re-bought every deal at `backend/app/services/sim_session.py:220-233`. **Severity: should-fix.**

9. **Second-tab semantics for the Labels toggle and stale blind-check dialog are undefined.** Evidence: the spec invokes second tabs at `docs/ai-dlc/specs/two-mode-simulate.md:61-62` but only promises reload persistence for the toggle at `:76`; current localStorage-backed controls read once into React state at `frontend/src/components/SimulateView.tsx:94-104,160-170`. Two tabs can show different label states, and a tab holding the pending dialog will not learn that another tab submitted unless it reloads or retries. Specify whether state is tab-local or session-wide; if session-wide, storage-event/refetch behavior is required. **Severity: should-fix.**

10. **The contract map’s content inventory is factually inaccurate, though the full-note gate avoids an additional Simulate leak.** Evidence: `docs/ai-dlc/contracts/persona-label-toggle.md:115-125` claims three named lead lines and no other content pack with literal archetypes; `content/preflop/exploit.json:53,67,81,95,109,123,166` contains seven named leads, with more names at `:55,69`, and `content/cards/preflop.json:106` names four archetypes. This does not add a fifth Simulate render site because the entire exploit note is already gated, but the evidence document should not be relied on for prose counts. **Severity: optional.**

11. **Baseline verification could not be completed cleanly.** Evidence: `backend/tests/test_detection_probe.py:74-88` consumes a pinned local corpus, and `backend/tools/detection_corpus.py:555-557` rejects its session because it has no completed hands. `./scripts/verify.sh` ended with 2 failures and 2,189 passes, so it never printed `BACKEND VERIFY OK`; these failures appear unrelated to this unbuilt spec. Ruff and frontend strict TypeScript checks passed. **Severity: optional.**
