# Blind review r1 — P3b spec (Claude refuter, Opus, 2026-09-22)

Returned inline and saved verbatim by the Director. Reviewed: spec rev 1, tickets rev 1, contract map and measurement, against the worktree at `4168687`.

VERDICT: FAIL

1. **blocking** — The portrait block stacks ~465px of chrome above a 239px felt. Spec item 1 puts BOTH `.topbar` and `.simulate .sim-topbar` at `order: 0`. From the measurement's figures — masthead 181px, `.sim-topbar` 388×284px, `.stage` 388×239px, dock fixed 113px — the felt in session fits above the dock by 34px at 412×915, but at 360×800 (dock top 687) the bottom of the table renders under the fixed action bar, and item 4's touch floor makes the cluster taller still. Fix: keep `.simulate .sim-topbar { order: 2 }` in portrait, or add a Verify-by leg "in-session portrait at 360×800: `.stage` bottom ≤ dock top".
2. **blocking** — The 44px floor under the EXISTING gate is not free in landscape: `.mode-chip` (Practice) is inside `.mode-groups`, a direct flex child of `.app` at order 0 (`App.tsx:436`), ABOVE the order-2 masthead; `.btn.history-filter` is likewise order-0 content. Raising `.mode-chip` from 24px (`app.css:1393`) to 44px adds ~18px per chip row above the drill table on a 412px-tall landscape screen. Fix: scope those two to portrait, or add a landscape Practice leg.
3. **should-fix** — `.tt-opt { min-height: 44px }` clips the theme labels and targets a non-control: `.tt-opt` is an aria-hidden span inside `.tt-track { width:132px; height:40px; overflow:hidden }` (`app.css:94-104`, `App.tsx:373`); the real target is `button.theme-toggle`. Fix: floor on `.theme-toggle` only.
4. **should-fix** — `.sim-speed-input` is an invisible absolute overlay (`app.css:3491-3497`, `inset: 0; opacity: 0`); a min-height on it creates an 18px transparent click-catcher below the 26px pill. Fix: floor on `.sim-speed-face` (`app.css:3498-3502`).
5. **should-fix** — Item 2's `.nav-tabs` reset is incomplete: the gate sets nine declarations (`app.css:6797-6815`) including `margin: 0`, a 56px `padding-inline-start`, `background`, `border-top`, `border-bottom`, `inset`, `z-index`. Fix: enumerate the declarations to revert.
6. **should-fix** — The rotate hint's placement contradicts the measurement it cites (`measurement:76-77` assumed `.sim-topbar { order: 2 }`). Fix: state the insertion point — inside `.sim-main` immediately before `<SimTable>` (`SimulateView.tsx:1330-1332`).
7. **should-fix** — `.statstrip { overflow-x: visible }` with the base `overflow: hidden` shorthand (`app.css:1483`) computes to `auto` and adds a scrollbar while unclipping the rounded corners. Fix: rely on `flex-wrap: wrap` alone.
8. **optional** — `.history-hand-tier { min-width: 0 }` is inert (`flex: none`, no min-width, `app.css:5077-5080`); `flex-wrap: wrap` alone cures the 12px. Verify-by (b) should compare element `scrollWidth` to `clientWidth`, never `innerWidth` (a classic scrollbar offsets ~15px in headless Chromium).

Cleared by the reviewer, not re-opened: cascade (every overridden gate rule is equal specificity and earlier; `.nav-tabs.nav-tabs-open` and `.nav-reveal[aria-expanded]` set values portrait wants anyway); Biome (`noDescendingSpecificity` stays at 18 hits with both the portrait block and the touch floor materialised; `biome ci` exits 0 at warn); nav JS state (closed is CSS `display: none` only, class toggle at `App.tsx:399`, focus call on a hidden element is a no-op, Escape handler binds only while open); media-query algebra (the dropped `max-height` leg is unreachable in portrait; Android Chrome's keyboard resizes the visual viewport only, and Simulate has no text input); the 128px constant's three homes are untouched and no item-4 control lives inside the dock; the 9px nodes on every screen are the masthead's `.new-tag`, outside the three components item 6 names. Side effect noted: with `.nav-reveal` hidden in portrait the 128px strip is dead space on non-Simulate routes.

Checks run: frontend gate green on the untouched worktree (biome, tsc, vitest 84/84); cascade specificity audit; Biome trial on scratch copies with the proposed rules; nav state trace in `App.tsx`.
