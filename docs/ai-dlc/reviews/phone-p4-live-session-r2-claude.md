# Fan-in review r2 — P4 diff (Claude refuter, Opus, 2026-09-22)

Returned inline by the reviewer (it declined to add an untracked file to a worktree whose cleanliness
it had verified) and saved verbatim by the Director. Reviewed: the uncommitted working-tree diff on
`feat/phone-p4-live-session` at `d642d30` plus four untracked files, against spec rev 2.

VERDICT: FAIL

1. **should-fix** — `notice` survives Leave and reappears on a brand-new table: `SimulateView.tsx:536-545` (`clearStored`) and `:560-565` (`askForMode`) clear the id and the token but never `setNotice(null)`, and `chooseMode`→`adopt` at `:578` doesn't either — so 409 → Leave → sit down again renders "Acted elsewhere — table refreshed." over a freshly minted table no other device has ever seen, announced by `role="status"`. Fix: `setNotice(null)` inside `clearStored`, beside the two refs it already clears together.
2. **optional** — `sim_session.py:1050-1057`: parsing `HandState` now precedes the `status != "in_progress"` check, so a completed hand with corrupt `state_json` raises pydantic's `ValidationError` (still a 400, since it subclasses `ValueError`) instead of the clean `"no hand in progress"`.
3. **optional** — `sim_session.py:1226-1231`: `_view()` reading the post-UPDATE `hand` relies on `expire_on_commit` defaulting to True; nothing sets it False today. An explicit `db.expire(hand)` gains immunity to a future session-factory change; costs one line.
4. **optional** — `deal_next_hand` (`:1609-1616`) and `leave_session` (`:1754-1763`) get the token check but no compare-and-set, so two same-token deals, or a Leave landing during another request's grading `await`, can still both write. Unreachable on today's single-worker, no-I/O-grader setup, and the spec scopes the compare-and-set to `apply_hero_action` only.
5. **optional, cosmetic** — `app.css:6969-6976`: under the phone gate the notice sits at `bottom: 128px` while the comment at `:6941` records a 113px dock; with a shove armed the `.sim-action-warn` block (`:6987`, `bottom: 100%` of the dock) can run under the notice, which paints over it at `z-index: 21`.
6. **note, not a defect** — the Director's docs ticket (roadmap, ledger, `log.md`, Resume) is absent from this diff; the spec's Definition of Done requires it in the same PR.

Everything the brief prioritised checks out. `rowcount` is read before `db.commit()` and the rollback discards the `SimDecision`/`DrillAttempt`/seat-settlement rows; the ORM `hand.state_json`/`hand.status` writes are genuinely deleted, not supplemented; the compare uses the exact string read (`observed_state_json = hand.state_json`, no re-serialisation); token checks precede the turn guard, the legality check and the leave write on all three routes, and the only untokened state-mutating route is `blind-check`, which spec item 6a exempts by design. `GET /session/current` is declared before `/session/{session_id}`, resolves as one SELECT over `sim_session` with a correlated `max(SimHand.created_at)` scalar subquery plus `.first()` — it does not materialise the hand table. Domain core, migrations, `Decision`, `App.tsx` and dependencies are all untouched; the three route-level test files are updated only by threading the token through, never by loosening an assertion.

Checks run: targeted pytest on the four touched test files → 57 passed. Full backend suite `pytest -q -p no:randomly` → 2306 passed, 2 skipped (7m23s). `ruff check .` clean; `mypy app` → Success, 69 files. `make check-frontend` → Biome clean (105 warnings all pre-existing), tsc clean, vitest 84/84, vite build OK. Concurrency test proven load-bearing: on an rsync copy under `$TMPDIR` with the compare-and-set replaced by the old ORM assignment plus a stub `rowcount = 1`, `test_two_concurrent_submits_grade_exactly_one_decision` fails with `assert [200, 200] == [200, 409]` and only that test fails; the unmodified file passed 8× consecutively. `git status --short` and `git diff --stat` identical before and after the review.
