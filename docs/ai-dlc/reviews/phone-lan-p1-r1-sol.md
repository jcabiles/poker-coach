FAIL

1. **Blocking — The launcher does not guarantee the global requirement that the backend port remains on loopback, meaning reachable only from the Mac itself.**

   - Evidence: [scripts/serve.sh:116](/Users/johncabiles/Documents/Github/poker-coach/scripts/serve.sh:116) omits `--host`, while installed Uvicorn enables `UVICORN_*` environment overrides at [uvicorn/main.py:61](/Users/johncabiles/Documents/Github/poker-coach/backend/.venv/lib/python3.14/site-packages/uvicorn/main.py:61). The process matcher at [scripts/serve.sh:33](/Users/johncabiles/Documents/Github/poker-coach/scripts/serve.sh:33) does not check the effective bind address. This contradicts [phone-lan-p1.md:37](/Users/johncabiles/Documents/Github/poker-coach/docs/ai-dlc/specs/phone-lan-p1.md:37), [phone-lan-p1.md:27](/Users/johncabiles/Documents/Github/poker-coach/docs/ai-dlc/contracts/phone-lan-p1.md:27), and the global no-go at [phone-and-6max.md:189](/Users/johncabiles/Documents/Github/poker-coach/docs/ai-dlc/roadmap/phone-and-6max.md:189).
   - Failure scenario: With `UVICORN_HOST=0.0.0.0 poker-coach restart --lan`, Uvicorn binds port 8008 to every interface, while `status` still recognizes and reports it as this repository’s backend.
   - Recommendation: Pass `--host 127.0.0.1` explicitly. This enforces the invariant regardless of shell state; the cost is one launcher argument and corresponding plan-text updates.

2. **Blocking — The claim that the unauthenticated API “never appears on the wifi” is incompatible with the proposed proxy architecture.**

   - Evidence: The assertion appears at [spec:15-20](/Users/johncabiles/Documents/Github/poker-coach/docs/ai-dlc/specs/phone-lan-p1.md:15) and [roadmap:72-80](/Users/johncabiles/Documents/Github/poker-coach/docs/ai-dlc/roadmap/phone-and-6max.md:72), but [frontend/vite.config.ts:16](/Users/johncabiles/Documents/Github/poker-coach/frontend/vite.config.ts:16) forwards every `/api` request received by the LAN-bound Vite server to the backend. The verification at [spec:180-185](/Users/johncabiles/Documents/Github/poker-coach/docs/ai-dlc/specs/phone-lan-p1.md:180) checks only port 8008’s listening socket.
   - Failure scenario: Port 8008 listens only on `127.0.0.1`, so the socket check passes, while another home-network device successfully calls `http://<mac-ip>:7777/api/v1/health` or posts to `/api/v1/simulate/session` through Vite.
   - Recommendation: State the real boundary: the backend listener remains loopback-only, but its API is intentionally reachable through Vite on the trusted home network. This makes the security claim and acceptance test truthful at documentation-only cost; preventing API access would make phone play impossible under the no-auth scope.

3. **Blocking — The two-client measurement can report a coherent hand after a stale client action has been applied to the wrong decision point.**

   - Evidence: The proposed query at [spec:124-130](/Users/johncabiles/Documents/Github/poker-coach/docs/ai-dlc/specs/phone-lan-p1.md:124) detects duplicate ordinals only; it neither detects gaps nor validates which state an action targeted. The request contains only a `Decision` at [frontend/src/api/client.ts:126](/Users/johncabiles/Documents/Github/poker-coach/frontend/src/api/client.ts:126), while the server reloads whatever state is current at [sim_session.py:970-978](/Users/johncabiles/Documents/Github/poker-coach/backend/app/services/sim_session.py:970). Moreover, `sim_hand` contains one current `state_json`, not a continuous event line, at [models.py:79-92](/Users/johncabiles/Documents/Github/poker-coach/backend/app/db/models.py:79), contrary to [roadmap:80-83](/Users/johncabiles/Documents/Github/poker-coach/docs/ai-dlc/roadmap/phone-and-6max.md:80).
   - Failure scenario: Both tabs show a preflop decision facing an open; tab A calls and advances to a flop decision facing a bet, then stale tab B submits its preflop-intended fold. The server legally applies that fold on the flop, stores unique ordinals 0 and 1, and the proposed SQL returns no rows.
   - Recommendation: Record both clients’ observed decision points, both HTTP results, and the resulting state and rows; classify any accepted stale action or failed simultaneous request as a finding. This makes the measurement capable of deciding whether the cross-device-session slice needs version protection, at the cost of modestly richer probe output.

4. **Should-fix — The verification order leaves a LAN-bound stack in place, allowing both the early-return and argument-forwarding checks to pass without exercising their claimed states.**

   - Evidence: [spec:180-189](/Users/johncabiles/Documents/Github/poker-coach/docs/ai-dlc/specs/phone-lan-p1.md:180) starts loopback, changes it to LAN mode, then claims to test `start --lan` against loopback and tests the global launcher without first stopping the LAN stack.
   - Failure scenario: If `bin/poker-coach` still drops `--lan` as it currently does at [bin/poker-coach:17](/Users/johncabiles/Documents/Github/poker-coach/bin/poker-coach:17), step 5 invokes ordinary `start` against the already-running LAN stack; the socket state remains correct and the check can be reported successful.
   - Recommendation: Run the loopback early-return test immediately after the loopback start, then stop both servers and test the global launcher from a clean state. The gain is a non-false-positive lifecycle test; the cost is two extra stop/start operations.

5. **Should-fix — The isolated frontend instruction relies on `FRONTEND_PORT=7778`, but Vite does not read that variable directly.**

   - Evidence: The referenced acceptance ticket instructs starting Vite with `FRONTEND_PORT=7778` at [tickets/phone-lan-p1.md:130-135](/Users/johncabiles/Documents/Github/poker-coach/docs/ai-dlc/tickets/phone-lan-p1.md:130), while [frontend/vite.config.ts:14](/Users/johncabiles/Documents/Github/poker-coach/frontend/vite.config.ts:14) hardcodes 7777; only `scripts/serve.sh` translates `FRONTEND_PORT` into Vite’s `--port` argument at [scripts/serve.sh:126](/Users/johncabiles/Documents/Github/poker-coach/scripts/serve.sh:126).
   - Failure scenario: `BACKEND_PORT=8123 FRONTEND_PORT=7778 npm run dev` from the main frontend still attempts port 7777 and either collides with the owner’s stack or probes the wrong frontend.
   - Recommendation: Give the explicit direct command using `vite --port 7778 --strictPort`, while starting only the isolated worktree backend. This makes the measurement reproducible and avoids touching the owner’s backend; the cost is one concrete command in the plan.

6. **Should-fix — The stale-document acceptance commands cannot pass because retained planning and historical files intentionally contain the phrases and deleted filename they prohibit.**

   - Evidence: [tickets/phone-lan-p1.md:115-117](/Users/johncabiles/Documents/Github/poker-coach/docs/ai-dlc/tickets/phone-lan-p1.md:115) requires every “governing initiative” match to name phone-and-6max and no reference to the deleted draft, but the retained roadmap references that deletion at [phone-and-6max.md:76](/Users/johncabiles/Documents/Github/poker-coach/docs/ai-dlc/roadmap/phone-and-6max.md:76), and the spec itself references it at [phone-lan-p1.md:85](/Users/johncabiles/Documents/Github/poker-coach/docs/ai-dlc/specs/phone-lan-p1.md:85).
   - Failure scenario: The builder performs every intended document edit and deletion, but the prescribed repository-wide greps still return the spec, roadmap, ticket, ledger, historical review, and bot-realism roadmap, so the plan’s Definition of Done cannot be satisfied.
   - Recommendation: Scope the checks to the two banners being corrected, or search for the exact stale assertions rather than generic historical language. This produces an executable gate at the cost of changing two grep commands.

Verified without finding: the current early-return citation correctly points to `scripts/serve.sh:110-112`; the frontend contains no browser-side absolute API URL; the database path is derived from the imported source tree at `backend/app/db/session.py:9-13` with no runtime override; the frontend matcher continues to recognize Vite with an added `--host`; and scalar-based argument parsing can be implemented safely under Bash 3.2 with `set -euo pipefail`.
