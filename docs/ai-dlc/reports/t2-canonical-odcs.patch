--- a/contracts/poker_events.odcs.yaml	2026-08-02 00:28:59
+++ b/contracts/poker_events.odcs.yaml	2026-08-06 16:42:56
@@ -10,7 +10,7 @@
 kind: DataContract
 id: poker-events
 name: Poker simulation events
-version: 1.0.0
+version: 1.1.0
 status: active
 domain: poker-analytics
 dataProduct: poker-analytics warehouse
@@ -358,6 +358,45 @@
         logicalTypeOptions:
           minimum: 0
           maximum: 100
+      - name: engine_node_key
+        logicalType: string
+        physicalType: VARCHAR
+        required: false
+        description: >
+          T2 (flywheel S3): determinism-guard grouping key. Postflop rows
+          reuse the domain's `postflop_node_key` (flat, raise, cbet_dry,
+          cbet_wet, cbet_mono, turn_barrel, river_value). Preflop rows carry
+          an export-side-only facing-state label (unopened, vs_limpers,
+          vs_raise, vs_3bet_plus) derived from public action history, never
+          domain logic. NULL on action='post' (forced blinds) — posts are
+          excluded from determinism contexts.
+        quality:
+          - type: sql
+            description: engine_node_key is NULL on action='post' rows only.
+            query: >
+              SELECT COUNT(*) FROM decisions
+              WHERE (action = 'post') <> (engine_node_key IS NULL)
+            mustBe: 0
+      - name: hand_class_bucket
+        logicalType: string
+        physicalType: VARCHAR
+        required: false
+        description: >
+          T2 (flywheel S3): hand-class label. Preflop rows: a coarse
+          export-side-only hole-card bucket (pair, suited-ace,
+          suited-broadway, offsuit-broadway, other), derived from the
+          acting seat's hole cards. Postflop rows: the domain's
+          `strength_bucket` (made-hand ladder + draw category), encoded
+          "<strength>|<draw>" e.g. "top_pair|weak", "monster|none" — see
+          `docs/ai-dlc/reports/t2-export-report.md` for the full enum list.
+          NULL only on action='post' rows (forced blinds).
+        quality:
+          - type: sql
+            description: hand_class_bucket is NULL on action='post' rows only.
+            query: >
+              SELECT COUNT(*) FROM decisions
+              WHERE (action = 'post') <> (hand_class_bucket IS NULL)
+            mustBe: 0
       - name: exported_at
         logicalType: string
         physicalType: VARCHAR
